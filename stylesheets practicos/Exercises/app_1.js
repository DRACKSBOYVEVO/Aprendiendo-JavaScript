//Obtener el nombre de archivo de una URL en JavaScript
//const FileName = (url: string): string => url.substring(url.lastIndexOf('/') + 1);
FileName('http://domain.com/path/to/document.pdf'); // 'document.pdf' 